using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveCheese1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3(2.14f, 1.32f, -2.38f);
        print("Cheese Moved");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
